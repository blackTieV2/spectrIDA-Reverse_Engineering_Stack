# consolidation/

Background curation workflow: DETECT recurring lessons → PROPOSE a
consolidated record → HUMAN REVIEW → PROMOTE. Never auto-promote.

Candidates land in `candidates/` with `status: candidate`,
`authority: candidate`, `canonical: false` until a human reviews them with
`shared/templates/memory-promotion-review.template.md`.
