#!/usr/bin/env python3
"""search_recall_memory — workspace utility (STUB).

Interface
---------
python search_recall_memory.py "<query>" [--tier 1-4]

Intended behaviour
------------------
Search recall candidates: deterministic routing, exact, full-text, metadata/relationship filters. Results are suggestions; authority is verified separately.

Status: documented stub. Implement when the workspace workflow needs it.
Deviations from spec v1.0: implemented as Python for cross-platform use
(the operator's machine is Windows; .ps1 wrappers may be added later).
"""
import sys

if __name__ == "__main__":
    sys.exit("search_recall_memory: stub — see module docstring for the intended interface.")
