#!/usr/bin/env python3
"""propose_memory_consolidation — workspace utility (STUB).

Interface
---------
python propose_memory_consolidation.py --candidate "<title>" --episodes ep-...

Intended behaviour
------------------
Write a candidate record into retrieval/consolidation/candidates/ with status: candidate, authority: candidate, canonical: false. Human review required for promotion.

Status: documented stub. Implement when the workspace workflow needs it.
Deviations from spec v1.0: implemented as Python for cross-platform use
(the operator's machine is Windows; .ps1 wrappers may be added later).
"""
import sys

if __name__ == "__main__":
    sys.exit("propose_memory_consolidation: stub — see module docstring for the intended interface.")
