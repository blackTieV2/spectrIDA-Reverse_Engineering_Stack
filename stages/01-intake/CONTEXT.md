# Stage 01-intake — contract

**Purpose:** Establish goal, constraints, stakeholders, success criteria, target repository/system, unresolved requirements.

**Hard rule:** No implementation unless explicitly approved. Output: intake brief.

**Read before acting:** root `AGENTS.md` → `PROJECT_STATUS.md` → this file.

**Tools:** see `_config/tool-profiles.yaml` (`intake` profile).

**Output:** `stages/01-intake/output/` only.

**Stop conditions:** scope exceeded; live state contradicts the task packet;
execution hold becomes active; approval missing for a consequential step.
