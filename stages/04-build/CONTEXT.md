# Stage 04-build — contract

**Purpose:** Implement only the approved design and scope.

**Hard rule:** Requires: verified live target, TARGET GATE PASS, explicit build approval, execution hold clear, working-tree ownership clear.

**Read before acting:** root `AGENTS.md` → `PROJECT_STATUS.md` → this file.

**Tools:** see `_config/tool-profiles.yaml` (`build` profile).

**Output:** `stages/04-build/output/` only.

**Stop conditions:** scope exceeded; live state contradicts the task packet;
execution hold becomes active; approval missing for a consequential step.
