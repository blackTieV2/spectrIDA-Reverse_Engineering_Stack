# Stage 03-design — contract

**Purpose:** Convert approved requirements + evidence into an implementation design: scope, acceptance criteria, rollback, risks.

**Hard rule:** Design does not authorise build. Output: design doc.

**Read before acting:** root `AGENTS.md` → `PROJECT_STATUS.md` → this file.

**Tools:** see `_config/tool-profiles.yaml` (`design` profile).

**Output:** `stages/03-design/output/` only.

**Stop conditions:** scope exceeded; live state contradicts the task packet;
execution hold becomes active; approval missing for a consequential step.
