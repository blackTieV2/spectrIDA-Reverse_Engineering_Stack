# skills/

Project-local agent skills (task-specific procedures packaged for reuse).
Each skill is a directory with a `SKILL.md`. Skills are procedural memory —
proposals to change them go through human review like any procedure.
