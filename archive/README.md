# archive/

Retired material. Nothing here is current truth. Notable contents:

- `atlas_extension.py.retired` — the old in-memory REGraph MCP extension,
  superseded by the graph-consistent integration in `spectrida/mcp_server.py`.
